// import { values } from "./values";
import { semantics } from "./semantics";

export const media = {
  // Icon
  icon: {
    color: "inherit",
    hover: {
      color: "inherit",
    },
  },
  // Text
  text: semantics.text,
};
